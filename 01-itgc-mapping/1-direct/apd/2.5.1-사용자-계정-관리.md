# 2.5.1 사용자 계정 관리

## 1. ISMS 원본

### 인증기준
정보시스템 사용자 계정의 등록·변경·삭제·잠금 절차를 수립·운영하고, 신규/직무변경/퇴직 시점에 적시 반영한다.

### 개요·사례
- **적용 범위**: ERP·HR·DB·OS·네트워크 장비 등 모든 정보시스템 사용자 계정
- **대표 통제 활동**: 신청서 → 승인 결재 → 시스템 발급 → 적시 회수 → 휴면계정 정리 → 직무분리
- **빈출 결함 패턴**:
  - 퇴직자 계정 미회수 (가장 빈번)
  - 신청서·승인 결재 없이 발급
  - 신청자=승인자=처리자 (직무분리 위반)
  - 공용 계정 무분별 사용 (관리자 1명이 admin 비번 공유)

## 2. ITGC 매핑

| 항목 | 값 |
|---|---|
| 거리감 | 직접 (1-direct) |
| ITGC 영역 | APD (Access to Programs and Data) |
| 부영역 | — |
| 보조 영역 | ELC/personnel (퇴직자 회수는 인사-IT 인계 entity-level 결함과 연결) |

## 3. IT감사에서는 이렇게

| ISMS 요구 | ITGC 테스트 절차 | 모집단 | 샘플 |
|---|---|---|---|
| 1. 등록·변경·삭제·잠금 절차 문서화 | 정책·절차 매뉴얼 입수 → walkthrough | — | — |
| 2. 신청 → 승인 → 처리 분리 | 권한 매트릭스 → 신청·승인·처리 동시 보유자 식별 | 전 권한 보유자 | 100% |
| 3-A. 신규 발급 적시성 | 신청서·승인 결재·시스템 발급 일자 정합 | 감사기간 신규 발급 계정 | 샘플 |
| 3-B. 퇴직자 회수 적시성 | HR 퇴직자 명단 vs 시스템 사용자 마스터 대조, 회수 일자 ≤ 퇴직 일자 + N일 | 감사기간 퇴직자 | 100% |
| 4. 휴면계정 점검 | 휴면계정 잠금 절차 증빙 + 최근 로그인 일자 추출 | 전 사용자 | 100% (또는 N개월 미접속자 추출) |
| 5. 공용 계정 통제 | 공용 계정 list + 사용자 식별 가능성(개별 비번·로그) 검증 | 공용 계정 전체 | 100% |

## 4. Q&A

### Q1. 휴면계정 N개월 기준이 회사마다 다른데, 무엇을 기준으로?
A. ISMS는 "주기적 점검·잠금" 요구만. 구체 N 명시 X. **회사 내부 정책 문서화 + 일관 적용 입증**이 ITGC 검증 포인트. 일반적 시장 관행: 90일(보안 강한 금융권) ~ 180일(일반 업종). ITGC 결함 판단 = 정책과의 불일치이지 N 자체 절대값 X.

### Q2. 공용 계정은 무조건 결함인가?
A. 무조건 X. **부득이한 사유 + 사용자 추적 가능 + 비밀번호 별도 관리**가 충족되면 OK. 결함 판정 = 추적 불가능한 공용계정 (예: admin 1개로 5명 공용, 비번 공유, 로그상 누가 썼는지 모름). 클라우드/DBA root 계정 같이 기술적으로 공용이 불가피한 경우엔 jump server·sudo 로그·세션 녹화 등 보완통제로 추적성 확보 필요.

### Q3. 메모리 덤프 막아야 하나? ITGC에서 어디까지 봐?

A. **일반 사용자 = 막는 게 표준, 특수권한자 = 사용 추적까지.**

- **왜 막나**: 메모리 덤프 → Mimikatz 등으로 인증 토큰·세션 키·평문 비밀번호·암호화 키 추출. 도메인 전체 장악 가능
- **권한 단위**:
  - Windows = `SeDebugPrivilege`
  - Linux = `CAP_SYS_PTRACE` 또는 `/proc/[pid]/mem` 접근권
  - DB = 메모리 dump 가능한 sysadmin/DBA 권한
- **ITGC 검증 범위 (현실)**:
  - 일반 비금융 ITGC = OS/DB 특수권한 보유자 list + 회수 절차 + 사용 로그 검토까지. 메모리 덤프 차단 자체 구현은 보안조직 영역으로 push
  - ISMS-P 인증 받을 클라이언트 / 금융권 = 메모리 덤프 차단 정책·구현·로그까지 직접 검증 필요
- **결함 판단**:
  - 일반 사용자에게 덤프 권한 부여 → 즉시 결함
  - 특수권한자라도 사용 로그 X / 정기 검토 X → 결함
  - 부여 사유 문서화 X → 결함
- **2.5.1에서 다룰 이유**: 매핑상 2.5.5 특수권한이지만, ITGC 권한 매트릭스 검토 시 **일반 계정에 덤프 권한 섞여 있는지 확인이 2.5.1 신청-승인-처리 검증의 첫 step**임

### Q4. SSO·IDM 도입 클라이언트는 테스트 어떻게? 화이트박스/블랙박스?

A. **블랙박스 100% 테스트가 표준. 결함 발견 시 화이트박스로 root cause 추적.**

- **블랙박스 (1순위)**:
  - 모집단: 감사기간 HR 퇴직자 100%
  - 테스트: 시스템 사용자 마스터에서 회수 일자 ≤ 퇴직 일자 + N일 (회사 정책)
  - 자동화든 수기든 결과만 봄. SSO/IDM 도입했다고 통제 면제 X
  - 시간 효율 + 회계감사 ITGC 표준 절차

- **화이트박스 (보완)**:
  - 블랙박스에서 결함 발견 시 root cause 추적용
  - 검토 대상: IDM 워크플로우 정의 (트리거 조건·예외 처리), HR ↔ IDM 연계 인터페이스, 실패 알림 설정
  - 단독 사용 시 시간 무한 빨림. 권장 X

- **추가 step — IDM 자체 통제 검증** (메타 통제):
  - IDM admin 권한 보유자 list + 회수 + 사용 로그 (슈퍼유저급, 2.5.5 특수권한과 연결)
  - 자동화 실패 알림 수신자·대응 절차 검증
  - HR → IDM 연계 인터페이스 권한 (서비스 계정 통제)
  - **IDM 자체에 결함 = 모든 시스템 권한 통제 무력화 = 가장 중대한 결함**

- **결함 판단**:
  - 자동화 도입 = 검토 면제로 본 경우 → 즉시 결함
  - IDM 자체 권한 관리 X → 즉시 결함
  - 자동화 실패 알림 미설정 → 결함 (실패가 묻힘)
  - 블랙박스 결과 OK + 화이트박스 추가 검증 안 한 경우 → 결함 X (시간 효율 차원에서 인정)

### Q5. 신청서 부재 = 무조건 결함?
A. 원칙적으로 결함. 다만 **시스템상 권한 변경 이력(audit log) + 승인 결재 증빙**이 신청서 역할 대체하면 보완 인정 가능. 단, 회사 정책상 신청서 의무 명시되어 있는데 없으면 결함 (정책 위반).

## 관련 인증기준
- 2.5.5 특수 계정 및 권한 관리 (특수권한 분리)
- 2.5.6 접근권한 검토 (정기 검토)
- 2.2.5 퇴직 및 직무변경 관리 (인사 entity-level 절차)

## 관련 회계감사기준서 / 글로벌 표준
- 회계감사기준서 315 (IT 위험 이해)
- COBIT DSS05 / DSS06 (Access Management)
